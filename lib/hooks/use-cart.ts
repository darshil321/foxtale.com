// import React from 'react';

// function useCart({}) {
//   const cart = useSelector(cart); // fetch cart
//   const totalAmount = cart.products.reduce((acc, cur) => {
//     acc += cur.quantity * cur.price;
//   }, 0);

//   const tax = calculateTax(product);
//   const shipping = shipping();

//   return {
//     totalAmount,
//     tax,
//     shipping
//   };
// }

// export default useCart;

// // getProductPrice = (p) => {
// //   return p.price * package.quantity.toFixed(2)
// // }
